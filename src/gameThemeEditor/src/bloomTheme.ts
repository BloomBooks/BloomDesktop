// Bloom UI theme tokens, COPIED (not imported) from src/BloomBrowserUI/bloomMaterialUITheme.ts
// and utils/colorUtils.ts so this editor stays a self-contained project that depends on nothing
// in BloomBrowserUI. Keep these in sync with Bloom if its palette changes.
export const bloom = {
    blue: "#1d94a4", // kBloomBlue — primary
    blueText: "#19818f", // kBloomBlueTextBackground — darker blue for contrast
    purple: "#96668f", // kBloomPurple — secondary
    red: "#d65649", // kBloomRed
    gold: "#f3aa18", // kBloomGold
    panel: "#2e2e2e", // kPanelBackground
    fontStack: "Roboto, NotoSans, sans-serif", // kUiFontStack
};
