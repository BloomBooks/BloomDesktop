This folder marks the Wall Calendar as a source of pages for the Add Page dialog
(see PageTemplatesApi.GetBookTemplatePaths). Bloom generates the page thumbnails
into it on demand; nothing else needs to be checked in here.
