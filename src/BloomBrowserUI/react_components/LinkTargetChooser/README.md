`./show.sh` should open a browser with this component for you to play with, using mock data`./show-with-bloom.sh` should use Bloom itself, which is expected to be running on port 8089
`./test.sh` should run tests against mock data
