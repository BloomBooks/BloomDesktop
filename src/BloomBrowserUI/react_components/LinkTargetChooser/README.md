`./show.sh` opens a browser with this component for you to play with, using mock data
`./show-with-bloom.sh` uses Bloom itself, which is expected to be running on port 8089
`./test.sh` runs playwright tests against mock data
