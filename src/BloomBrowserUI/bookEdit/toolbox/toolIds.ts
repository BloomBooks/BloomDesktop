// Note: This class was created to have a short, easy to reference class
// that wouldn't cause a lot of other things to get dragged into your bundle.
// (Referencing a static variable in the relevant tool would cause a lot of things to get dragged into certain bundles)
export class ToolIds {
    // ENHANCE: As needed, add other tool IDs (and have the relevant tool reference this)
    public static kOverlayToolId = "overlay";
}
